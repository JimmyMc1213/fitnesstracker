# App boot smoke, passes with or without Supabase (RN-2 auth gate)
# Run from apps/mobile: npm run test:e2e
appId: app.newyouai.mobile
---
- launchApp
- extendedWaitUntil:
    visible:
      text: "NewYou"
    timeout: 60000
- assertVisible: "NewYou"
