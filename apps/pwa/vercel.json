{
  "buildCommand": "npm run build:sunset",
  "outputDirectory": "dist",
  "installCommand": "echo 'PWA retired — redirect only'",
  "ignoreCommand": "exit 1",
  "rewrites": [
    { "source": "/auth/callback", "destination": "/auth/callback/index.html" }
  ]
}
